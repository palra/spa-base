# Configuration files : router.js
# ===================
# 
# As the file is named `router.js`, the `module.exports` will be the future
# `config.router̀  global.
# This file defines all settings relating to routing.

module.exports =
    
    # strict: are `/foo` and `/foo/` different ?
    strict: false
    
    # caseSensitive: enable case sensitivity, ie. are `/foo` and `/Foo` the same ?
    caseSensitive: false